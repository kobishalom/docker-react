sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t kobishalom/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true kobishalom/docker-react npm run test
